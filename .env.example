# Backend Configuration
JWT_SECRET="secret123"

# Note: VITE_API_URL is currently hardcoded in docker-compose.yml as http://localhost:5000/api
# If you need to change it for LAN access, update it there or modify the docker-compose to use variables.
